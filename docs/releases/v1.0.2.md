# GO Fitness 1.0.2 (build 3)

- Display resource availability weekdays, local hours and validity dates before booking, for both staff and members.
- Display session start and end times in the branch timezone instead of relative timestamps.
- Require loaded availability before submitting a court booking, with an explicit retry action on loading failure.
- Backend dependency: deploy the additive `availabilityRules` and `timezone` fields on the existing self-service resource list.
- Next also displays these periods for member bookings and all staff resource types.
- Verified with 28 Flutter tests, two Next formatter tests, and seven focused backend tests. Physical Android/iOS validation remains separate.
- Keep all older Android archives intact.
